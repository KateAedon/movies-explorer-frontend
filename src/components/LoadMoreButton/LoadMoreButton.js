import React from 'react';
import './LoadMoreButton.css';

function LoadMoreButton() {
    return (
        <section className='load-more'>
            <button className='load-more_button' type='button'>Ещё</button>
        </section>
    );
}

export default LoadMoreButton;